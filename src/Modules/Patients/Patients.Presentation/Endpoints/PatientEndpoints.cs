using Microsoft.AspNetCore.Routing;

namespace Patients.Presentation.Endpoints;

public static class PatientEndpoints
{
    // Placeholder for the module-shell checkpoint. Replaced by the real POST /api/patients
    // route when the Patients vertical slice is built.
    public static IEndpointRouteBuilder MapPatientEndpoints(this IEndpointRouteBuilder endpoints) => endpoints;
}
